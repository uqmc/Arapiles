import React from "react";
import PrimaryLayout from "../layouts/primaryLayout";

const Home = () => {
  return(
    <PrimaryLayout>
      <h1>Main Page</h1>
    </PrimaryLayout>
  );
};

export default Home;
