import React from "react";
import PrimaryLayout from "../../layouts/primaryLayout";

const StartHere = () => {
    return (
        <PrimaryLayout> 
            Skills book home
        </PrimaryLayout>
    );
};

export default StartHere;
