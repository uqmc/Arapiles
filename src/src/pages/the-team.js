import React from "react";
import PrimaryLayout from "../layouts/primaryLayout";

const TheTeam = () => {

  return (
    <PrimaryLayout>
      Execs array
    </PrimaryLayout>
  );
};

export default TheTeam;
