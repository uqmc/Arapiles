import React from "react";
import PrimaryLayout from "../layouts/primaryLayout";

const Home = () => {
    return (
        <PrimaryLayout> 
            Home
        </PrimaryLayout>
    );
};

export default Home;
