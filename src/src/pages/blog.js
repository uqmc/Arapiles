import React from "react";
import PrimaryLayout from "../layouts/primaryLayout";

const Blog = () => {
    return (
        <PrimaryLayout> 
            Blog
        </PrimaryLayout>
    );
};

export default Blog;
